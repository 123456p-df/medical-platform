# Docker Compose only. Use a URL-safe random password (letters, digits, '-' and '_').
POSTGRES_PASSWORD=
# Only required for compose.gpu.yaml; complete downloaded NVIDIA model directory.
NV_SEGMENT_CT_HOST_DIR=
