# 整理日志

## 2026-09-10 GitHub 交付

- 用户指定桌面 `/Users/123456p/Desktop/Medical/`，远程仓库为 `https://github.com/123456p-df/medical-platform.git`，目标分支为 `main`。
- 推送前执行远程同步检查，确认本地 `main` 与 `origin/main` 无分叉；新增远程 `Front-End` 分支未改动。
- 构建与现有影像类型、3D 体积回归检查均已通过；暂存内容通过 `git diff --check` 和凭据模式扫描。
- 发现 `backend/.env.example` 的 `AI_API_KEY` 含凭据形态值且已存在于远程历史，已在当前版本清空；待确认项：用户需到对应服务商后台撤销并重新生成该历史密钥。
- 提交 `e48f7c9`（`Improve patient workspace and local preview access`）已推送到 `origin/main`。
- `.vite/`、额外 `medical/V3/` 同源副本均保留在本地但排除提交；未删除、移动或重命名任何文件。

## 2026-09-10 MONAI 辅助诊断接入盘点（只读）

- 只读核对桌面 Medical 的前后端辅助诊断链路；未修改桌面源码、配置或运行状态。
- 现有能力：NIfTI CT/MRI 上传与切片读取、CT 器官分割异步任务、NV-Segment-CT/VISTA3D 适配器、GLB 生成、影像审核工作流、病历文本 AI 问答。
- 关键缺口：当前 AI 状态明确为 `image_analysis: false`；前端 findings API 仍返回空数组；后端没有病灶分析任务、检测框/置信度持久化、finding 查询与审核接口，也没有把模型坐标映射回切片叠加层。
- 建议首个单部位 MVP 聚焦胸部 CT 肺结节候选检测，新增 MONAI `lung_nodule_ct_detection` Bundle；保留现有 NV-Segment-CT 负责肺/器官分割，两者职责分开。
- 待确认项：运行模型的 GPU 环境、目标数据格式（当前后端只接收 NIfTI）、是否先按研究演示范围实现肺结节候选检测与医生复核。

## 2026-09-10 Profile、患者管理与影像审核重组

- 以用户刚从 GitHub pull 的桌面 `/Users/123456p/Desktop/Medical/` 为只读基准，在项目内新建 `medical/V3/` 工作副本；盘点确认当前为 Vue 3 + TypeScript + Vite 前端，原始桌面代码未先行修改或删除。
- 从医生和患者侧栏移除“个人资料”独立入口；右上角头像继续作为唯一 Profile 入口，并增强悬停提示。Profile 页面新增返回工作台入口，重做表单层级、输入框、焦点态、占位文本与保存区；本地预览资料可保存到浏览器。
- 患者工作台将姓名/ID 搜索提升到患者列表正上方，新增“加入患者”按钮，并在已选患者操作区加入删除入口。本地预览支持新增、软移除与撤销，数据保存在浏览器中，不删除病历或影像文件。
- Overview 时间线为 Pending Review 明确显示“开始审核”；影像页将审核状态、AI Findings 入口与“确认审核完成”按钮移动到影像画布上方，并保存本地审核状态。
- 工作副本与桌面目录均通过 TypeScript/Vite 生产构建；11 个关键修改文件比对一致，桌面 4173 的工作台、Profile、影像审核三条路由均返回 HTTP 200。
- 未删除、移动或重命名任何原始文件。待确认项：无。已知范围：本地示例 CT/MRI 切片接口仍依赖未启动的后端，界面管理与审核状态使用本地预览数据。

## 2026-09-10 Git pull 后 Vite 模块解析恢复

- 只读检查桌面 `/Users/123456p/Desktop/Medical/`：`src/stores/auth.ts` 存在且已被 Git 跟踪，`vite.config.ts` 与 `tsconfig.app.json` 的 `@` 路径别名配置正确，相关源码没有未提交冲突。
- 定位到 pull 替换源码后，旧 Vite 开发进程仍保留失效的模块解析/监听状态，导致已存在的 `@/stores/auth` 被错误报告为无法解析。
- 未修改刚 pull 下来的源码；仅停止旧的 4173 开发进程，并从桌面当前 `main` 代码以本地免密码预览模式重新启动。
- 验证：`/src/router/index.ts` 已返回 HTTP 200，并正确解析为 `/src/stores/auth.ts`。未删除任何文件；待确认项：无。

## 2026-09-09 macOS 运行副本准备

- 只读盘点了 `/Users/123456p/Downloads/medical-platform-main 2/`，确认网页位于 `medical-platform/Medical/`，桌面目标为 `/Users/123456p/Desktop/Medical/`。
- 在当前项目内创建 `medical/V2/` 副本，未修改下载目录原始文件；复制了前端源码、public 模型、测试样本和 Vite 配置。
- 在副本内补充 macOS `start.command`、`pnpm start` / `pnpm serve` 命令、构建说明和 Vite preview 固定端口配置。
- 待确认项：桌面 `Medical/` 目录包含已有根目录和 `V1/` 工作副本；本轮计划仅覆盖桌面根目录的前端文件，保留现有 `V1/` 和不在源项目中的历史内容，不执行删除。
- 遇到的问题：当前环境没有检测到 Docker 或 PostgreSQL；前端可独立构建和启动，但真实登录需要后端与数据库。
- 桌面同步：已将 `medical/V2/` 前端内容覆盖同步到 `/Users/123456p/Desktop/Medical/` 根目录；保留桌面原有 `V1/`、缓存和历史内容，未删除文件。
- 验证：桌面目录 `vite build` 成功；临时预览 `http://127.0.0.1:4183` 首页 HTTP 返回正常。临时预览服务已停止，桌面原有 `4173` 服务未中断。
- 启动脚本：`start.command` 已改为等待 Vite 就绪后再打开浏览器，并在退出时停止本次启动的开发服务器；已通过 `zsh -n` 语法检查。
- 依赖检查：桌面目录的离线安装已复用 155 个缓存包，但当前沙箱网络限制导致 pnpm 的附加元数据检查未完成；桌面构建仍已使用同一份依赖内容成功完成。启动脚本直接调用已安装的 Vite 文件，首次在网络正常的 Mac 上会自动执行完整安装。
- 最新版本打开：已从桌面 `Medical/` 根目录的最新 `dist/` 启动临时预览，并在浏览器打开 `http://127.0.0.1:4185/login`；当前显示最新源项目的 `Sign in to PulmoLink` 页面，未使用旧版 `V1`。
- 下一轮建议：如需登录和真实患者数据，补齐后端 Python 依赖与 PostgreSQL，再运行后端并使用 `VMRB_BACKEND_URL` 配置前端代理。

## 2026-09-09 本地预览免密码入口

- 用户要求直接访问本机网页；确认最新桌面根目录版本没有可用后端，普通登录会停留在登录页。
- 在工作副本和桌面根目录加入 `VITE_LOCAL_PREVIEW` 本地预览模式：双击 `start.command` 时自动进入医生工作台，并加载源码自带的合成患者、检查、病历和待办数据。
- 正常启动/后端模式仍使用原有密码登录；下载目录原始项目未修改。
- 已通过 `vue-tsc`、Vite 构建和浏览器验证；当前已打开 `http://127.0.0.1:4185/doctor/dashboard`，页面显示 5 名合成患者和 1 条待办。
- 已停止旧的 `V1`/旧根目录开发服务，释放 `4173`；最新桌面根目录版本已用 `VITE_LOCAL_PREVIEW=true` 启动并打开 `http://127.0.0.1:4173/doctor/dashboard`，确认页面显示 5 名合成患者和 1 条待办。

## 2026-09-09 医生端导航与患者工作台合并

- 将医生端“工作台”和“患者”合并为唯一的“患者工作台”入口；侧栏移除重复的患者、影像、报告、医疗档案和数字人体快捷入口。
- 工作台显示全部患者，保留姓名/编号搜索及影像类型、器官、状态、风险、日期筛选，并新增当前结果数/患者总数显示。
- `/doctor/patients` 旧列表地址重定向至工作台；点击患者后进入该患者详情，标签顺序调整为概览、影像、报告、AI 助手、3D 影像。
- 患者详情返回按钮改为“返回工作台”；影像和报告不再依赖侧栏中的隐式默认患者。
- 已同步桌面 `/Users/123456p/Desktop/Medical/`，工作副本与桌面 6 个关键修改文件一致；两处 TypeScript/Vite 构建通过，运行地址 HTTP 200，当前开发服务已通过 HMR 加载更新。
- 异常记录：同步后首次构建命令在项目根目录额外刷新了根目录 `dist/` 和构建缓存；未删除文件，桌面目标随后单独构建验证成功。

## 2026-09-09 侧栏折叠与 3D 精准浏览

- 在 `medical/V2/` 工作副本加入桌面侧栏收起/展开按钮；折叠后保留图标导航，并记录用户上次选择。手机端继续使用原有抽屉导航。
- 3D 器官导航改为可拖拽分栏：拖动人体窗口右侧分隔条可调整宽度，双击恢复默认，左右方向键可微调；宽度设置会保存在本机。
- 数字人体启用鼠标位置缩放和右键平移，将最小观察距离从 2.3 调整到 0.45；双击人体或器官可把该位置设为观察中心，实现局部精准放大。
- 工作副本已通过 `vue-tsc` 与 Vite 生产构建；更新已覆盖同步到桌面 `/Users/123456p/Desktop/Medical/`，五个关键源码文件比对一致，桌面目录再次构建成功。
- 桌面最新版开发服务位于 `http://127.0.0.1:4173`，HTTP 状态为 200；未删除、移动或重命名任何原始文件；待确认项：无。

## 2026-09-08 运行项目

- 只读盘点根目录，确认是 Vite + Vue 3 + TypeScript 的 PulmoLink 本地网页版。
- 检测到 `4173` 已被桌面版开发服务器占用；未中断该进程。
- 在项目内维护副本 `medical/V1/` 启动开发服务器，实际监听地址为 `http://localhost:4174/`。
- 本次仅运行，未修改、移动、重命名或删除任何原始文件。
- 待确认项：无。

## 2026-09-08 患者管理、个人资料、助手与模型修订

- 只读盘点根目录及 medical/：原始 src/、配置、README、dist/、依赖、日志和现有 V1 / 旧版副本均在原处。
- 桌面 V1 与项目内 V1 源码一致。建立 `medical/V1-before-patient-profile-20260908/` 本轮前副本，在现有 V1 工作副本实施。
- 本轮范围：患者新增 / 可恢复删除接口与界面、个人资料及头像 / 附件、已处理事项收起、悬浮 AI 对话壳与接口约定、数字人体改进、公开 CT / MRI 样本验证。
- 删除患者采用状态标记并提供恢复入口，影像与报告原文件保留；不进行文件删除。
- 待确认项：AI 服务地址与鉴权后续接入，本轮先完成交互壳。下一轮：功能与真实样本验证后同步桌面 V1。

## 2026-09-08 交互与双语修订：验证与交付

- 交付结果：已同步 `/Users/123456p/Desktop/Medical/V1/`；源码与 Vite 配置逐项比对一致；桌面目录最终 `pnpm build` 成功（13.51 秒）。工作副本构建及模态回归通过。旧版源码与保留的备份一致。

- 修改范围：项目内现有 `medical/V1/` 副本；桌面 V1 同步前再次确认源码与原始根目录一致，并建立 `medical/V1-before-interaction-fixes-20260908/` 旧版副本。
- 完成：通知面板、已读与跳转；ABO / Rh(D) 分开展示；AI 确认、修改、忽略、撤销与本地保存；报告新建、草稿、签署、重置、患者端可见；文稿式 Doctor Review 与实时预览；全站中英文切换。
- 影像：实际上传检查与示例检查分开；DICOM 元数据识别 CT / MR / DX，未知类型手选、混合类型提示分批；文件使用 IndexedDB 保留；普通图片独立显示。修复 Cornerstone 的 Worker 预构建、解码器模块导出、元数据及画布高度问题。
- 浏览器验证（独立测试地址 127.0.0.1:4180）：通知展开 / 全部已读 / 跳转；AI 确认 / 修改 / 忽略 / 刷新；报告编辑 / 重置 / 草稿 / 无已有报告时新建并签署 / 刷新保留 / 患者端可见；中文与英文切换；实际 DICOM 像素渲染；取消上传保留原影像；上传一种只展示对应记录；普通图片手选 X-Ray 后正常加载。
- 模态回归脚本 8 项通过，覆盖 CT / MR / DX 元数据、普通图片未知类型、文件名提示、损坏元数据、压缩包筛除、混合类型提示。
- 保留全部原始文件及历史构建产物，无删除操作。测试文件与测试浏览器数据均为合成内容；测试浏览器数据与桌面默认端口的用户数据隔离。
- 待确认项：无。已知范围：当前仍为浏览器本地演示应用，未接入后台 AI；导入支持 DICOM 和常规位图，压缩包需解压，NIfTI / NRRD 尚未实现。
- 下一轮建议：用用户自己的 CT / MRI / X-Ray 样本继续验证压缩传输语法与多帧序列。

## 2026-09-07 交互与双语修订：只读盘点

- 根目录可见内容：README.md、cleaning-log.md、index.html、package.json、pnpm-lock.yaml、pnpm-workspace.yaml、src/、medical/、dist/、node_modules/、tsconfig.app.json、tsconfig.json、tsconfig.node.json、两个 tsbuildinfo 文件、vite.config.ts。
- src/ 为 Vue + TypeScript 源码；medical/V1/ 是已建立的工作副本；dist/ 为构建产物；node_modules/ 为依赖；其余为说明、日志与构建配置。
- 用户指定的桌面 V1 与项目内 medical/V1 源码一致。本次在项目内现有副本实施，完成验证后同步用户指定的 V1。
- 已识别：通知缺少交互、AI 修改没有编辑表单、状态对象复用、报告缺省时签署直接返回、上传混入示例检查、语言均硬编码。
- 未移动、重命名或删除任何原始文件。待确认项：无。后续：修复交互、双语与影像检查归属，验证构建与页面。

## 2026-09-07

### 本次操作摘要

- 只读检查了 `/Users/123456p/Downloads/medical-platform-main/` 源仓库结构。
- 确认当前工作目录已有一份同源的 PulmoLink 前端工程。
- 将本地前端整理为无后端依赖的浏览器演示版。
- 补充了 `README.md` 和 `cleaning-log.md`。
- 调整了 `package.json` 启动脚本和 `vite.config.ts` 的预览监听地址，使其兼容 macOS、Windows 和 Linux。
- 执行了 `pnpm build`，生产构建成功。
- 在本地浏览器中验证了登录页、医生门户和患者门户；数字人体和肺部 3D 画布均正常创建，窄屏布局可正常访问。

### 待确认项

- 无。

### 遇到的问题或异常

- 构建时 Cornerstone 的 WASM 依赖出现 Node 模块外部化提示，属于浏览器打包正常提示，不影响运行。

### 下一轮建议

- 如需要连接真实后端，可以在此基础上把 API 客户端从 mock 数据切换回 `/api/v1` 服务。
- 如需要部署到局域网或服务器，可使用 `pnpm build && pnpm serve`，或在 `dist/` 前配置支持 SPA 路由的静态服务器。

## 2026-09-07 补充

### 本次操作摘要

- 按用户要求，在当前项目内新建了 `medical/V1/`。
- 将前端源码、Vite 配置、包管理文件、README 和整理日志复制到 `medical/V1/`。
- 在 `medical/V1/` 内执行了 `pnpm install` 和 `pnpm build`，均成功。
- 原始根目录文件保持只读，未删除或移动。

### 待确认项

- 无。

### 遇到的问题或异常

- 无。

### 下一轮建议

- 后续可以只维护 `medical/V1/`，根目录的同源文件是否保留由用户决定。

## 2026-09-07 第二次补充

### 本次操作摘要

- 用户确认目标目录是 `/Users/123456p/Desktop/Medical/`。
- 已将 `medical/V1/` 中的网页源码和配置复制到 `/Users/123456p/Desktop/Medical/V1/`。
- 在目标目录内执行了 `pnpm install` 和 `pnpm build`，均成功。
- 当前开发服务器已切换到 `/Users/123456p/Desktop/Medical/V1/`。

### 待确认项

- 无。

### 遇到的问题或异常

- 无。

### 下一轮建议

- 后续以 `/Users/123456p/Desktop/Medical/V1/` 为准。
## 2026-09-10 患者工作台删除与性能优化

- 只读盘点根目录与 `medical/V3/`，确认 `medical/V3/` 为本轮工作副本；未移动、重命名或删除任何原始文件。
- 修复患者工作台删除目标固定显示首位患者的问题：表格每一行新增绑定该患者 ID/姓名的独立删除入口；单击行改为选中并刷新右侧预览，箭头或双击进入患者档案。
- 优化筛选体验：增加清除筛选、无结果恢复入口、加载状态、加载失败重试；修复年龄未登记显示和状态/风险中英文混排。
- 将工作台之外的路由改为按需加载；初始 JavaScript 包由约 834 kB 降至约 156 kB，3D 模块进入页面时再加载。
- `medical/V3/` 与桌面 `/Users/123456p/Desktop/Medical/` 均通过 `vue-tsc -b && vite build`；桌面 4173 开发服务器已确认返回新版患者表格源码。
- 同步时曾因相对路径多创建 `/Users/123456p/Desktop/Medical/medical/V3/` 下的 7 个同源文件副本。按禁止删除规则保留，未参与桌面根项目运行；列入待确认项。
- 待确认项：如用户希望整理上述多余副本，需另行明确决定；本轮未执行删除。
- 下一轮建议：接入后端归档/恢复 API 后，为服务端模式补充可恢复删除和操作审计。
## 2026-09-10 本机自动登录

- 只读检查 `medical/V3/` 工作副本的认证 store、路由守卫、登录页、API 客户端和本地演示数据开关。
- 实际验证既有 `demo_doctor` 演示账号时，当前后端 `/api/v1/auth/login` 返回 HTTP 500，因此未将失效密码作为交付方案。
- 新增统一运行环境判断：在 `127.0.0.1` / `localhost` 的 Vite 开发模式自动创建本地医生演示会话并使用合成数据，访问 `/login` 会自动跳转医生工作台；设置 `VITE_LOCAL_PREVIEW=false` 可恢复真实后端认证。
- 防止本地演示令牌触发 401 全局退出；AI 助手在演示模式不再发起需要后端鉴权的状态请求。
- 工作副本与桌面 `/Users/123456p/Desktop/Medical/` 均完成构建；影像类型和 3D 体积回归脚本全部通过；4173 已确认提供新版运行环境模块。
- 未删除、移动或重命名任何文件。待确认项：后端登录 500 的数据库/服务端原因后续单独诊断。
